<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="metro.k.cover"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MetroCoverApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_metro_cover"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="metro.k.cover.tutorial.TutorialActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="metro.k.cover.lock.LockService"
            android:label="@string/app_name" />

        <activity
            android:name="metro.k.cover.lock.LockActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="metro.k.cover.SettingActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="metro.k.cover.lock.ChooseLockPattern"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="metro.k.cover.lock.SelectSecurityActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="metro.k.cover.wallpaper.WallpaperDetailActivity"
            android:configChanges="keyboardHidden|orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="metro.k.cover.wallpaper.WallpaperHomeeActivity"
            android:label="@string/app_name" >
        </activity>

        <receiver android:name="cmetro.k.cover.lock.LockReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.SCREEN_ON" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>